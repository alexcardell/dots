---
- name: Initialise
  include_role:
    name: initialise

- name: Setup LSP
  shell: './install.sh && yarn install --frozen-lockfile'
  args:
    chdir: '~/.vim/pack/bundle/start/coc.nvim'

- name: Setup LSP extensions
  shell: 'nvim -c "CocInstall -sync {{ " ".join(extensions) }} | qall"'
