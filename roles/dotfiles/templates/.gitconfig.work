# {{ ansible_managed }}
# vim: ft=gitconfig

{% if work %}
[user]
  name = {{ work_git_user_name }}
  email = {{ work_git_user_email }}
  signingkey = {{ work_git_user_signingkey }}
{% else %}
# Work user name or email was not defined
{% endif %}
